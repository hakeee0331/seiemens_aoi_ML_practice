# 인수인계 — 2026-08-24 (compact 직전 스냅샷)

이 문서는 컨텍스트 compact 이후 새 세션이 빠르게 따라잡기 위한 요약이다.
상세 수치·표는 각 실험 문서(`docs/experiments/*.md`)에 있으니 여기서는
**무엇을 왜 했고, 지금 뭘 하고 있고, 다음에 뭘 할지**만 압축한다.

## 지금 브랜치 상태

- 작업 브랜치: `sandbox/lsw/model-development` (origin에 push됨, `dev`와 동기화 완료 — 2026-08-24 기준)
- 워크플로우: `dev`에서 pull 받아 이 브랜치에서 작업 → 완료되면 **PR로** `dev`에 병합 (직접 push 지양, 팀원과 겹치는 작업이 많아서 가시성 확보용)
- **CONTRIBUTING.md / AGENTS.md / README.md는 건드리지 않는다** (사용자 지시). CLAUDE.md는 건드려도 됨.
- `docs/lsw/` 아래 개인 노트만 자유롭게 수정. 팀 공용 문서(`docs/model_val.md`, `docs/experiments/index.md`)는 다른 사람 행도 같이 있으니 병합 시 충돌 조심.
- `requirements.txt` 충돌 시 주의: 팀원이 `pip freeze` 전체 덤프(UTF-16 깨진 인코딩)를 커밋한 적 있음 — 우리 curated 버전을 지켰음. dev에서 다시 덮어써지면 같은 이슈 재확인 필요.

## 완료된 노트북 (lsw 작성분)

| 노트북 | 한 줄 요약 |
|---|---|
| `0823_lsw_001_eda` | NaN 결측 0건. 무효 컬럼(mapping.json 기준) 채움값이 type마다 다름(0 아닐 수 있음). 상수열·중복행 다수. |
| `0823_lsw_002_baseline` | 5분리 baseline 최초 버전. grid 기반 threshold 탐색에 버그 있었음(→ 003에서 exact search로 교체). |
| `0824_lsw_003_structure_comparison` | **통합모델 vs 5분리** 순수 비교. 통합모델은 전 유형에서 Volume Reduction ≈0%(무효), 5분리는 유형별 편차 큼. Partial pooling 임계값도 시도했지만 완전한 해법 아님. **이론상 최대 허용 Slip Rate**(비용 기준 상한) 계산 결과, 1:100에서도 13~50%까지 허용 — 지금 쓰는 1% 목표는 비용 최적화가 아니라 별도 안전기준임을 확인. |
| `0824_lsw_004_imbalance_handling` | 불균형 처리 4기법(class_weight/SMOTE/ADASYN/undersample) + 라벨 클렌징(dongjin 아이디어 차용) 비교. **노트북 12절에 003+004 전체를 합친 검사유형별 최적 조합 표 있음** — 이게 지금까지의 최종 결론. |

## 핵심 확정 사실 (틀리기 쉬우니 재확인 없이 재사용해도 됨)

1. **통합모델은 어느 유형·어느 비용비율에서도 최적이 아니다.** 5분리 구조가 항상 이긴다.
2. **비교는 항상 baseline 대비 ΔTN(이득)/ΔFN(손해) 원본 건수로 한다** (비율만 보지 않음) — `docs/lsw/project/scope_and_roadmap.md`에 규칙으로 명문화됨. 예측 1(불량의심)로 남는 TP/FP는 baseline과 동일 처리라 변화가 없고, 실제 변화는 TN/FN 두 칸에서만 생긴다는 게 근거.
3. **검사유형별 최적 조합(002+003+004 종합, `0824_lsw_004` 노트북 12절)**:
   - type0: label_cleansing (단, 유형 전체가 아니라 이 유형만 선택 적용해야 함)
   - type1/2/4: undersample
   - type3: 1:10에서 adasyn, 1:100에서 smote
   - (라벨 클렌징을 **전체 유형에 일괄 적용하면 pooled 기준 순손해** — type2에서 TN 3,072건 잃고 FN 10건만 줄어 나쁜 거래였음. type0은 반대로 단독으로는 이득이었는데 이전에 "type0이 손해 원인"이라고 잘못 정리했던 걸 정정함.)
4. **총비용 시나리오**: FP:FN = 1:10, 1:100 두 개를 병행 계산 (`scope_and_roadmap.md`에 확정).
5. **임계값 탐색은 "관측된 확률값 후보 exact search"를 쓴다** (0.01 grid 아님 — 해상도 문제로 이전에 버그 있었음).
6. **표본 부족 유형(type0, type4)은 결과 신뢰도가 낮다.** rule of three로 보면 Validation 불량 8건(type0)으로는 1% 목표를 통계적으로 검증할 근거가 원천적으로 없음(300건 필요). type4는 특히 Train(13건)보다 Val(60건) 표본이 더 많은 이상 패턴 — 아래 drift 항목 참고.

## 방금 발견한 것 (아직 노트북화 안 됨 — 다음 세션에서 이어갈 부분)

### Drift 확인 (kimjaehak의 `0823_kimjaehak_002_temporal_drift`가 이미 상당 부분 함)

- Train/Val/Test 불량 비율: **train 0.72% → val 0.45% → test 2.64%**(train의 3.67배). Validation에서 고른 임계값이 Test에서 안 맞는 근본 원인이 표본 부족뿐 아니라 **이 구조적 분포 이동 때문일 가능성**.
- 검사유형별 불량 비율(우리 자체 분할 기준, 확인 완료):

  | type | train | val | test |
  |---|---:|---:|---:|
  | 0 | 0.136% | 0.034% | 0.792% |
  | 1 | 1.630% | 2.277% | 7.055% |
  | 2 | 0.773% | 0.201% | 3.670% |
  | 3 | 0.767% | 0.088% | 1.971% |
  | 4 | 0.370% | **5.159%** | 3.439% |

  type0/2/3은 val까지 낮다가 test에서 급등(4~6배). type1은 꾸준히 우상향. **type4는 val이 유독 튀는 불규칙 패턴.**
- PSI(kimjaehak 결과): type4가 압도적(중앙값 0.989, 최댓값 4.25, 13개 중 11개가 "심각한 drift"). type0은 drift 거의 없음.

### 라벨 판정 기준 변화 (사용자 가설: "검사자가 시간에 따라 바뀌었다" — 검증 완료, 강하게 지지됨)

- 전체 데이터(440,274행, train/val/test 안 나눔)에서 **피처 완전 동일 + 라벨만 다른 모순 그룹 70개(140행)** 발견 (dongjin 방식: 전체 원본 피처 기준 그룹화).
- 시간순 정렬 시 **57개(81.4%)가 "먼저 0(정상)→나중에 1(불량)" 패턴**, 반대는 13개(18.6%)뿐. 우연이라 보기 어려운 비대칭.
- 유형별: type2가 28개로 가장 많음(9개는 반대 패턴) — **이게 004에서 라벨 클렌징이 type2를 가장 크게 해친 이유와 정확히 일치**. 즉 그때 지운 "모순"이 노이즈가 아니라 **검사 기준이 실제로 엄격해진 진짜 신호**였을 가능성이 큼 → 라벨 클렌징 관련 결론 재검토 필요할 수 있음.
- 0→1 그룹의 시간 간격 중앙값 약 40일(최소 11분~최대 125일) — 단기 재검수 실수가 아니라 장기 기준 변화로 보임.

### 다음 세션에서 할 일 (사용자와 논의 중이던 방향)

1. **005 노트북을 "drift + concept drift(라벨 기준 변화)" 주제로 만들기로 방향 논의 중** (아직 안 만듦). kimjaehak의 PSI 분석을 반복하지 말고, 그 위에 얹어서:
   - drift가 003/004의 임계값 불안정성을 실제로 설명하는지 연결
   - 라벨 판정 기준 변화(0→1 패턴)를 정량적으로 더 파기 (예: 주별로 얼마나 흔한지, 어떤 피처 구간에서 발생하는지)
   - type4처럼 표본 적은 유형은 PSI만으로 판단하지 말고 재검증
2. 원래 계획에 있던 **feature selection(mapping.json 마스킹)**은 이 drift 조사 다음으로 미뤄짐 — 여전히 할 일로 남아있음.
3. **dev의 dongjin/peace 작업과 계속 겹칠 수 있음** — 새 작업 시작 전에 `docs/experiments/index.md`, `docs/model_val.md`를 dev에서 다시 확인 권장. dongjin은 MoE(=5분리와 동일 개념), ADASYN, RuleFit, isolation forest, SHAP 등 이미 13개 실험 진행(단, 노트북 파일 자체는 3개만 repo에 있음 — `009_label_cleansing`, `013_adasyn`, `019_rule_injection`, 나머지는 문서 요약만 있고 검증 불가).
4. **dongjin의 pooled PR-AUC 지표는 5개 유형 모델의 예측을 그냥 합쳐서 계산한 값**이라 서로 다른 모델의 확률 스케일 비교 가능성이 검증 안 됨 — 절대 수치보다 방향성 참고용으로만 쓸 것.

## 반복해서 나온 교훈 (판단 기준으로 계속 재사용됨)

- **Ratio(교환비)가 아니라 총비용(또는 최소한 ΔTN/ΔFN)으로 최종 판단한다.** 비율은 "왜"를 이해하는 도구일 뿐.
- **Pooled(전체 합산) 지표만 보면 원인을 잘못 짚을 수 있다.** 반드시 유형별로 쪼개서 재확인한다(003 통합모델 사례, 004 라벨클렌징 type0/type2 사례에서 두 번 다 이걸로 실수했다가 정정함).
- **PR-AUC 개선 ≠ 실제 운영 지표(Volume Reduction/총비용) 개선.** 랭킹이 좋아져도 Slip Rate 기반 임계값이 불안정해지면서 상쇄되는 패턴이 반복 관찰됨(class_weight, 라벨클렌징 type2 등).
- **이상치 판단은 "다른 클래스보다 극단적인가"가 아니라 "같은 클래스 내에서도 극단적인가/라벨과 무관하게 드문가"로 해야 한다** — 처음 시도(정상+불량 섞은 IQR)가 틀렸었고, 두 번째 시도(정상 클래스만 기준)도 완전히 맞는 개념은 아니라는 지적을 받고 개념 정리만 해둔 상태(실제 재구현은 아직 안 함).
