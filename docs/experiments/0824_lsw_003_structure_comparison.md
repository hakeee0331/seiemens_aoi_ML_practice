# 0824_lsw_003_structure_comparison

## 연결된 노트북

`notebooks/0824_lsw_003_structure_comparison.ipynb`

## 상태

완료

## 목적

통합모델(전체 검사유형 하나로 학습) vs 검사유형별 5분리 모델 중 어느 구조가
나은지 순수하게 비교한다. 데이터 전처리(중복 제거, 시간순 6:2:2 분할)는
`main`의 `0824_kimjaehak_005_xgboost_baseline`과 동일하게 맞춰서, 구조 외
다른 변수가 섞이지 않게 했다.

## 이전 실험 대비 주요 변경사항

- `0824_kimjaehak_005_xgboost_baseline`의 데이터 처리(넓은 기준 중복 제거,
  timestamp 그룹 경계를 보존하는 6:2:2 분할)를 그대로 재사용했다 — 우리
  baseline(`0823_lsw_002`)의 전처리(좁은 기준 중복 제거, 유형별 독립 분할)는
  이번엔 쓰지 않았다.
- Train/Val/Test 시간 경계는 **전체 데이터 기준으로 한 번만** 계산하고, 두
  구조 모두 같은 경계를 공유한다.
- `mapping.json` 기반 피처 마스킹은 적용하지 않았다(구조와 독립적인 변수라
  다음 feature selection 단계로 미룸). 대신 학습 데이터 기준 상수열만 제거했다.
- 임계값 탐색을 `0823_lsw_002`의 `0.01` 간격 grid 대신, 실제 관측된 예측확률
  값을 후보로 쓰는 exact search로 교체했다(grid 해상도 문제 수정).
- 총비용은 `docs/lsw/project/scope_and_roadmap.md`의 두 시나리오(1:10, 1:100)를
  함께 계산했다.

## 평가 방법과 주요 결과

Test는 학습·모델 선택에 사용하지 않고 최종 평가에만 사용했다.

전체(pooled):

| 구조 | Slip Rate | Volume Reduction | 총비용(1:10) | 총비용(1:100) |
|---|---:|---:|---:|---:|
| 통합모델 | 0.22% | 0.09% | 76,132 | 76,582 |
| 5분리모델(pooled) | 3.78% | 31.4% | 53,079 | 60,729 |

**baseline(통합모델) 대비 ΔTN(이득)/ΔFN(손해) — 원본 건수 기준.** 예측
1로 남는 TP/FP는 baseline과 처리 결과가 동일하므로, 실제 변화는 TN/FN
두 칸에서만 생긴다:

| 방식 | TN | FN | ΔTN(통합 대비) | ΔFN(통합 대비) | 교환비 |
|---|---:|---:|---:|---:|---|
| 통합모델(baseline) | 65 | 5 | — | — | — |
| 5분리(유형별 임계값) | 23,918 | 85 | +23,853 | +80 | **약 298:1** |

검사유형별 (통합모델의 예측을 유형별로 다시 쪼개 계산):

| inspection_type | 통합 Slip Rate | 통합 Volume Reduction | 5분리 Slip Rate | 5분리 Volume Reduction |
|---|---:|---:|---:|---:|
| 0 | 0.00% | 0.00% | 16.54% | 53.19% |
| 1 | 0.64% | 0.63% | 1.02% | 18.10% |
| 2 | 0.00% | 0.00% | 1.71% | 18.93% |
| 3 | 0.00% | 0.00% | 7.13% | 32.35% |
| 4 | 0.00% | 0.00% | 0.00% | 0.00% |

- **통합모델은 "안전해서 좋은 게" 아니라 "모든 유형에서 똑같이 무효해서"
  안전하다.** 유형별로 쪼개보면 5개 유형 전부에서 Volume Reduction이
  0%에 가깝다(최댓값 type1의 0.63%) — 특정 유형에서 똑똑하게 걸러내는 게
  아니라 전 유형에 걸쳐 임계값이 똑같이 바닥까지 눌린 것이다. test negative
  총량(76,147)이 kimjaehak baseline과 정확히 일치해 재현성을 확인했다.
- **5분리모델은 4개 유형에서 실제로 의미 있는 신호를 찾아낸다**(Volume
  Reduction 18~53%). 다만 표본이 적은 유형(0, 2, 3)에서 Slip Rate가
  목표(1%)를 초과한다 — type0(Val 불량 8건) 16.5%, type2(32건) 1.7%,
  type3(24건) 7.1%. 표본이 가장 많은 type1(239건)만 목표에 근접(1.02%)하며
  Volume Reduction 18.1%도 확보했다.

### 상세 지표 (Confusion Matrix / PR-AUC)

**통합모델 vs 5분리(유형별 임계값)** — 검사유형별 원본 건수와 PR-AUC:

| type | 구조 | threshold | TN | FP | FN | TP | PR-AUC |
|---|---|---:|---:|---:|---:|---:|---:|
| 0 | 통합 | 1.79e-12 | 0 | 16,651 | 0 | 133 | 0.062 |
| 0 | 5분리 | 6.37e-06 | 8,857 | 7,794 | 22 | 111 | 0.067 |
| 1 | 통합 | 1.79e-12 | 65 | 10,263 | 5 | 779 | 0.263 |
| 1 | 5분리 | 2.54e-05 | 1,869 | 8,459 | 8 | 776 | 0.403 |
| 2 | 통합 | 1.79e-12 | 0 | 18,450 | 0 | 703 | **0.418** |
| 2 | 5분리 | 6.12e-06 | 3,492 | 14,958 | 12 | 691 | 0.357 |
| 3 | 통합 | 1.79e-12 | 0 | 29,988 | 0 | 603 | 0.189 |
| 3 | 5분리 | 6.98e-06 | 9,700 | 20,288 | 43 | 560 | 0.270 |
| 4 | 통합 | 1.79e-12 | 0 | 730 | 0 | 26 | **0.077** |
| 4 | 5분리 | 1.14e-06 | 0 | 730 | 0 | 26 | 0.023 |
| 합계 | 통합 | — | 65 | 76,082 | 5 | 2,244 | 0.237(전체 1개 값) |
| 합계 | 5분리(pooled) | — | 23,918 | 52,229 | 85 | 2,164 | 해당없음(유형별 상이) |

**주목할 점**: type2와 type4는 오히려 **통합모델의 PR-AUC가 5분리보다
높다**(type2: 0.418 vs 0.357, type4: 0.077 vs 0.023) — 순수 판별력(랭킹
능력)만 보면 통합모델이 이 두 유형에서 더 낫다는 뜻이다. 그런데도
Volume Reduction은 통합모델이 전 유형에서 0%에 가까웠다(위 표 참고) —
즉 **모델 자체의 랭킹 능력 문제가 아니라, 전 유형에 공통 임계값 하나를
적용하는 정책 문제**임을 다시 한번 보여준다. type1/3은 5분리가 PR-AUC도
더 높아(0.403>0.263, 0.270>0.189) 구조 자체의 이점도 있는 것으로 보인다.

**5분리 pooled 임계값(11절)** 적용 시 유형별 결과:

| type | threshold | TN | FP | FN | TP |
|---|---:|---:|---:|---:|---:|
| 0 | 1.92e-06 | 6,006 | 10,645 | 13 | 120 |
| 1 | 1.92e-06 | 0 | 10,328 | 0 | 784 |
| 2 | 1.92e-06 | 1,056 | 17,394 | 6 | 697 |
| 3 | 1.92e-06 | 2,890 | 27,098 | 17 | 586 |
| 4 | 1.92e-06 | 3 | 727 | 1 | 25 |

## 이론상 최대 허용 Slip Rate (참고용, 12절)

Slip Rate ≤1% 목표가 총비용 관점에서 얼마나 타이트한지 참고로 확인했다.
"오늘(전량 수동검사) 비용 = N×오검비용" 대비 "모델 적용 비용 =
FP×오검비용+FN×미검비용"이 더 싸지는 손익분기 조건에서, **이론상 최선
(Volume Reduction 100%)**을 가정할 때 감당 가능한 최대 Slip Rate는
`min(1, N/(k·P))`이다(N=실제 false call 수, P=실제 불량 수, k=비용비율).

| inspection_type | N/P | 최대 Slip Rate(1:10) | (1:20) | (1:100) |
|---|---:|---:|---:|---:|
| 0 | 125.2 | 100% | 100% | 100% |
| 1 | 13.2 | 100% | 65.9% | 13.2% |
| 2 | 26.2 | 100% | 100% | 26.2% |
| 3 | 49.7 | 100% | 100% | 49.7% |
| 4 | 28.1 | 100% | 100% | 28.1% |
| 전체 | 33.9 | 100% | 100% | 33.9% |

비용비율 1:100이라는 보수적 가정에서도 이론상 최대 허용 Slip Rate가
13~50%대로, 지금까지 목표로 쓴 1%보다 훨씬 높다. **1% 목표는 총비용
최소화에서 유도된 숫자가 아니라 별도의 안전/품질 기준(AQL류)으로 봐야
한다.** 이 수치는 참고 컨텍스트로만 남기고 이후 실험의 목표를 바꾸는 데는
쓰지 않는다.

## 결론과 다음 단계

상세 결론은 노트북 14절 참고. 임계값 선택 3가지 방식을 추가로 비교했다:

| 방식 | Slip Rate | Volume Reduction | 총비용(1:10) | 총비용(1:100) |
|---|---:|---:|---:|---:|
| 통합모델 | 0.22% | 0.09% | 76,132 | 76,582 |
| 5분리 (유형별 임계값) | 3.78% | 31.4% | 53,079 | 60,729 |
| 5분리 (pooled 임계값, partial pooling) | 1.65% | 13.1% | 66,562 | 69,892 |

- 유형별 Validation 표본이 적을수록(특히 type0: 8건) 통계적으로 1% 목표를
  그 유형 데이터만으로 검증할 근거가 없다(0건 실패 관찰 시 rule of three로
  95% 상한 ≈3/8=37.5% — 1%를 확인하려면 대략 300건 필요). 이 문제를
  해결하기 위해 5개 모델의 Validation 예측을 합쳐 임계값 하나만 고르는
  partial pooling을 시도했다.
- pooled 임계값은 전체 Slip Rate를 3.78%→1.65%로 목표에 가깝게 개선했지만
  Volume Reduction도 31.4%→13.1%로 크게 줄었다(편향-분산 트레이드오프).
  **type0은 pooled 임계값에서도 Slip Rate 9.77%로 여전히 목표를 초과**하고,
  대신 원래 안정적이던 type1의 Volume Reduction이 18.1%→0%로 희생됐다.
- 즉 세 방식 다 완전하지 않다: 통합(안전하지만 무효) / 유형별(효율적이지만
  위험) / pooled(중간이지만 여전히 불충분, 표본 큰 유형을 희생). 임계값
  방식을 바꾸는 것만으로는 type0의 근본적인 표본 부족 문제를 해결할 수 없다.
- 다음 단계(0824_lsw_004, 불균형 처리)에서는 임계값을 일단 **유형별 방식**
  (Volume Reduction이 가장 큼)을 기본값으로 유지하고, 오버샘플링/가중치로
  표본이 부족한 유형(0/2/3)의 유효 신호 자체를 늘릴 수 있는지를 핵심
  질문으로 삼는다.
- `mapping.json` 마스킹은 이후 feature selection 단계에서 적용한다.

## 저장된 모델 경로

- `models/0824_lsw_003_structure_comparison_unified.pkl`
- `models/0824_lsw_003_structure_comparison_type0.pkl`
- `models/0824_lsw_003_structure_comparison_type1.pkl`
- `models/0824_lsw_003_structure_comparison_type2.pkl`
- `models/0824_lsw_003_structure_comparison_type3.pkl`
- `models/0824_lsw_003_structure_comparison_type4.pkl`
